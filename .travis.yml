language: node_js

sudo: false

cache:  # 缓存
  apt: true
  directories: 
    - node_modules

node_js: stable #设置node相应的版本

install:
	- npm install -D #安装构建器依赖
	- cd ./test/smoke/template
	- npm install -D #安装模板项目依赖
	
scripts:
	- npm test